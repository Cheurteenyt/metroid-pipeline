name: Branch PR Automation (GLM 5.3 Flash)

# Politique de branches (docs/BRANCH_POLICY.md) :
#   une tache = une branche temporaire automation/glm-5.3-flash/<task-id>
#   -> PR automatique -> merge apres checks -> suppression de la branche.
# Fail-closed : si un check echoue, PAS de merge (PR laissee ouverte).
# Ce workflow ne modifie aucun script-porte du write relay.

on:
  push:
    branches-ignore:
      - main

permissions:
  contents: write
  pull-requests: write
  checks: read
  actions: read

concurrency:
  group: branch-pr-${{ github.ref_name }}
  cancel-in-progress: false

jobs:
  open-pr:
    if: >-
      startsWith(github.ref_name, 'automation/glm-5.3-flash/')
      && !contains(github.event.head_commit.message, '[ci-noop]')
    runs-on: ubuntu-latest
    timeout-minutes: 15
    env:
      BRANCH: ${{ github.ref_name }}
    steps:
      - name: Checkout complet
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          persist-credentials: true

      - name: Garde-fous
        run: |
          set -euo pipefail
          if [ "$BRANCH" = "main" ]; then
            echo "REFUS : main n'est jamais une branche de travail." >&2
            exit 1
          fi
          case "$BRANCH" in
            automation/glm-5.3-flash/*) ;;
            *) echo "REFUS : namespace non autorise ($BRANCH)." >&2; exit 1 ;;
          esac
          git ls-remote --exit-code --heads origin "$BRANCH" >/dev/null \
            || { echo "REFUS : branche absente sur origin." >&2; exit 1; }
          echo "Branche de travail : $BRANCH"

      - name: Detecter une PR existante
        id: find
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          if ! OUT=$(gh pr list --head "$BRANCH" --base main --state open --json number --jq '.[0].number // ""' 2>gh-find.err); then
            {
              echo "## gh pr list a echoue"
              echo '```'
              cat gh-find.err
              echo '```'
            } >> "$GITHUB_STEP_SUMMARY"
            cat gh-find.err >&2
            exit 1
          fi
          echo "PR ouverte existante : '${OUT:-aucune}'"
          echo "existing=$OUT" >> "$GITHUB_OUTPUT"

      - name: Creer la PR si besoin (GraphQL puis REST, diagnostic sur branche)
        id: create
        if: steps.find.outputs.existing == ''
        env:
          GH_TOKEN: ${{ github.token }}
          REPO: ${{ github.repository }}
        run: |
          set -euo pipefail
          SUBJECT=$(git log -1 --format=%s HEAD)
          {
            echo "## Resume"
            echo
            git log -1 --format=%b HEAD
            echo
            echo "## Fichiers touches"
            echo '```'
            git diff --name-status origin/main...HEAD
            echo '```'
            echo
            echo "## Tests executes"
            echo "- Validation locale avant push (revue du diff, syntaxe, aucun secret)."
            echo "- Porte de fusion : checks CI de cette PR (fail-closed)."
            echo
            echo "## Impact sur les proofs"
            echo "- Aucun changement des scripts-portes du relay ni du schema github-write-proof/v1."
            echo
            echo "## Provenance"
            echo "- Auteur du modele : GLM 5.3 Flash"
            echo "- Branche : \`$BRANCH\` -> base : \`main\`"
            echo "- request_id relay : n/a (travail de maintenance hors relay)"
            echo "- Methode de fusion : merge no-ff ; branche supprimee apres fusion."
          } > pr-body.md
          echo "Corps de PR construit : $(wc -l < pr-body.md) lignes."
          NUM=""
          if URL=$(gh pr create --base main --head "$BRANCH" --title "$SUBJECT" --body-file pr-body.md 2>gh-create.err); then
            NUM="${URL##*/}"
            echo "PR creee via gh pr create : $URL"
          else
            echo "gh pr create (GraphQL) a echoue — tentative REST de secours :" >&2
            cat gh-create.err >&2
            if NUM=$(gh api -X POST "repos/$REPO/pulls" \
                      -f title="$SUBJECT" -f head="$BRANCH" -f base=main \
                      -F body=@pr-body.md --jq '.number' 2>gh-create-rest.err); then
              echo "PR creee via REST : #$NUM"
            else
              {
                echo "## Echec de creation de PR"
                echo '```'
                cat gh-create.err
                echo '```'
                echo "## Echec du canal REST"
                echo '```'
                cat gh-create-rest.err
                echo '```'
              } >> "$GITHUB_STEP_SUMMARY"
              # Diagnostic persistant : commite sur la branche (lisible via git fetch),
              # message [ci-noop] pour ne pas re-declencher ce workflow.
              {
                echo "# Diagnostic branch-pr $(date -u '+%Y-%m-%dT%H:%M:%SZ')"
                echo
                echo "## gh pr create (GraphQL)"
                echo '```'
                cat gh-create.err
                echo '```'
                echo "## gh api REST"
                echo '```'
                cat gh-create-rest.err
                echo '```'
                echo "Piste : Settings -> Actions -> General -> Workflow permissions ->"
                echo "'Allow GitHub Actions to create and approve pull requests'."
              } > DIAGNOSTIC_PR.md
              git config user.name "GLM 5.3 Flash [bot]"
              git config user.email "glm53@local"
              git add DIAGNOSTIC_PR.md
              git commit -m "[ci-noop] diagnostic echec creation PR"
              git push origin "$BRANCH"
              echo "Diagnostic commite sur $BRANCH (DIAGNOSTIC_PR.md)." >&2
              exit 1
            fi
          fi
          echo "number=$NUM" >> "$GITHUB_OUTPUT"

      - name: Resoudre le numero de PR
        id: pr
        run: |
          set -euo pipefail
          EX="${{ steps.find.outputs.existing }}"
          CR="${{ steps.create.outputs.number }}"
          echo "number=${CR:-$EX}" >> "$GITHUB_OUTPUT"
          echo "PR ciblee : #${CR:-$EX}"

      - name: Attendre les checks externes (hors ce workflow)
        env:
          GH_TOKEN: ${{ github.token }}
          REPO: ${{ github.repository }}
        run: |
          set -euo pipefail
          HEAD_SHA=$(git rev-parse HEAD)
          DEADLINE=$((SECONDS + 300))
          sleep 45   # stabilisation : laisse les checks tardifs s'enregistrer
          while [ "$SECONDS" -lt "$DEADLINE" ]; do
            OUT=$(gh api -H "Accept: application/vnd.github+json" \
                  "repos/$REPO/commits/$HEAD_SHA/check-runs" \
                  --jq '.check_runs[] | select(.name != "open-pr") | .name + "=" + (.conclusion // "pending")' || true)
            FAIL=$(echo "$OUT" | grep -cE '=(failure|timed_out|startup_failure|action_required)$' || true)
            PEND=$(echo "$OUT" | grep -cE '=pending$' || true)
            if [ "${FAIL:-0}" -gt 0 ]; then
              echo "ECHEC DE CHECK — merge refuse (fail-closed) :" >&2
              echo "$OUT" >&2
              exit 1
            fi
            if [ "${PEND:-0}" -eq 0 ]; then
              echo "Aucun check externe en attente. Checks observes :"
              echo "${OUT:-<aucun>}"
              exit 0
            fi
            echo "En attente : $PEND check(s)..."
            sleep 20
          done
          echo "Timeout d'attente — merge refuse par prudence." >&2
          exit 1

      - name: Fusionner puis supprimer la branche
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          NUM="${{ steps.pr.outputs.number }}"
          gh pr merge "$NUM" --merge
          echo "PR #$NUM fusionnee (no-ff)."
          git push origin --delete "$BRANCH"
          echo "Branche $BRANCH supprimee sur origin."
          echo "=== Branches distantes restantes :"
          git ls-remote --heads origin
